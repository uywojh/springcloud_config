server:
  port: 8861

spring:
  application:
    name: cloud-config
  profiles: config-server1
  cloud:
    config:
      # 对应分支
      label: master
      server:
        git:
          # git地址
          uri: https://github.com/uywojh/springcloud_profile.git
          # 远程目录
          search-paths: cloud
          username: 664507439@qq.com
          password: wu15180180820

eureka:
  client:
    serviceUrl:
      defaultZone: http://172.16.246.128:8761/eureka,http://172.16.246.128:8762/eureka